model BugReport {
  id          String @id @default(cuid())
  description String
  history     String
  date        DateTime
  user        User?  @relation(fields: [userId], references: [id])
  userId      String
}
